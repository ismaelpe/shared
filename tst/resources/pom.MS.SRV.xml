<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.caixabank.absis.apps.service.cbk.insuranceproducts</groupId>
	<artifactId>ACCPR1-micro</artifactId>
	<version>2.7.0</version>
	<packaging>jar</packaging>
	<name>DEMO-micro</name>
	<description>Micro Service aplicativo</description>

	<parent>
		<groupId>com.caixabank.absis.arch.common</groupId>
		<artifactId>absis-app-micro-parent</artifactId>
		<version>1.19.1</version>
		<relativePath /><!-- lookup parent from repository -->
	</parent>

	<properties>
		<contract.package>com.caixabank.absis.apps.service.cbk.insuranceproducts.demo</contract.package>
		<absis.app.type>SRV.MS</absis.app.type>

		<!-- TAUX -->
		<tauxconnector-lib-starter.version>1.6.0</tauxconnector-lib-starter.version>
		<!-- ADS -->
		<adsconnector-lib-starter.version>1.6.0</adsconnector-lib-starter.version>
		<alf009cp.version>1.0.0</alf009cp.version>
		<rmr00021.version>2.0.0</rmr00021.version>
		<ass00023.version>0.0.0</ass00023.version>
		<clo00115.version>0.0.0</clo00115.version>
		<rmr00020.version>1.1.0</rmr00020.version>
		
		<!--  ABSIS2SI a2connecto2 -->
		<a2connector-spring-boot-starter.version>1.7.0</a2connector-spring-boot-starter.version>
		<segdecesosSimulacionOpenNow-si.version>1.2.2</segdecesosSimulacionOpenNow-si.version>
		<coordinaGobiernoTrazabilidad-si.version>10.10.2</coordinaGobiernoTrazabilidad-si.version>
		<fichaclientellamadasexternas-si.version>1.1.15</fichaclientellamadasexternas-si.version>
		<colectivoClienteOperativaCLI-si.version>5.1.1</colectivoClienteOperativaCLI-si.version>
		<cuestionarioVidaConsulta-si.version>1.2.3</cuestionarioVidaConsulta-si.version>
		<stopAndGoPosponer-si.version>24.1.22</stopAndGoPosponer-si.version>

		<!-- Librerias y Micros Caixa -->
		<workcenterquery.version>1.3.0</workcenterquery.version>
		<gtsbck-micro-contract.version>1.0.0</gtsbck-micro-contract.version>
		
	</properties>


	<dependencies>
		<dependency>
			<groupId>com.github.tomakehurst</groupId>
			<artifactId>wiremock-standalone</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>com.caixabank.absis.arch.common</groupId>
			<artifactId>absis-spring-cache-starter</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
			<exclusions>
				<exclusion>
					<groupId>org.junit.vintage</groupId>
					<artifactId>junit-vintage-engine</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<!-- BEGIN CONNECT GT Library -->
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpclient</artifactId>
		</dependency>

		<!-- END CONNECT GT Library -->

		<!-- BEGIN ADS Library -->
		<dependency>
			<groupId>com.caixabank.absis.arch.backend.ads</groupId>
			<artifactId>adsconnector-lib-starter</artifactId>
			<version>${adsconnector-lib-starter.version}</version>
		</dependency>

		<dependency>
			<groupId>com.caixabank.absis.ads.transaction</groupId>
			<artifactId>ads-alf009cp-lib</artifactId>
			<version>${alf009cp.version}</version>
		</dependency>

		<dependency>
			<groupId>com.caixabank.absis.ads.transaction</groupId>
			<artifactId>ads-rmr00020-lib</artifactId>
			<version>${rmr00020.version}</version>
		</dependency>

		<dependency>
			<groupId>com.caixabank.absis.ads.transaction</groupId>
			<artifactId>ads-rmr00021-lib</artifactId>
			<version>${rmr00021.version}</version>
		</dependency>

		<dependency>
			<groupId>com.caixabank.absis.ads.transaction</groupId>
			<artifactId>ads-ass00023-lib</artifactId>
			<version>${ass00023.version}</version>
		</dependency>
		
		<dependency>
            <groupId>com.caixabank.absis.ads.transaction</groupId>
            <artifactId>ads-clo00115-lib</artifactId>
            <version>${clo00115.version}</version>
        </dependency>
        
		<!-- END ADS Library -->

		<!-- BEGIN TAUX connector -->
		<dependency>
			<groupId>com.caixabank.absis.arch.backend.connectors.taux</groupId>
			<artifactId>tauxconnector-lib-starter</artifactId>
			<version>${tauxconnector-lib-starter.version}</version>
		</dependency>
		<!-- END TAUX connector -->

		<!-- BEGIN absis2 SI connector -->
		<dependency>
			<groupId>com.caixabank.absis.arch.backend.absis2</groupId>
			<artifactId>a2connector-spring-boot-starter</artifactId>
			<version>${a2connector-spring-boot-starter.version}</version>
		</dependency>

		<dependency>
			<groupId>es.lacaixa.absis.si.mca</groupId>
			<artifactId>FichaClienteLlamadasExternas_os_to</artifactId>
			<version>${fichaclientellamadasexternas-si.version}</version>
		</dependency>
		
		<dependency>
			<groupId>es.lacaixa.absis.si.mca</groupId>
			<artifactId>CuestionariovidaConsulta_os_to</artifactId>
			<version>${cuestionarioVidaConsulta-si.version}</version>
		</dependency>

		<dependency>
			<groupId>es.lacaixa.absis.si.mca</groupId>
			<artifactId>ColectivoClienteOperativaCLI_os_to</artifactId>
			<version>${colectivoClienteOperativaCLI-si.version}</version>
		</dependency>

		<dependency>
			<groupId>es.lacaixa.absis.si.mca</groupId>
			<artifactId>StopAndGoPosponer_os_to</artifactId>
			<version>${stopAndGoPosponer-si.version}</version>
		</dependency>
			
		<dependency>
			<groupId>es.lacaixa.absis.si.mca</groupId>
			<artifactId>SegdecesosSimulacionOpenNow_os_to</artifactId>
			<version>${segdecesosSimulacionOpenNow-si.version}</version>
		</dependency>
		
		<dependency>
			<groupId>es.lacaixa.absis.si.mca</groupId>
			<artifactId>CoordinaGobiernoTrazabilidad_os_to</artifactId>
			<version>${coordinaGobiernoTrazabilidad-si.version}</version>
		</dependency>
		<!-- END absis2 SI connector -->

		<!-- BEGIN LIBRERIAS CAIXA -->
		<dependency>
			<groupId>com.caixabank.absis.apps.lib.cbk.WorkCenterQuery</groupId>
			<artifactId>WorkCenterQuery-lib</artifactId>
			<version>${workcenterquery.version}</version>
			<exclusions>
				<!-- Se excluye la dependencia con la TAUX para no tener dos versiones 
					de la librería -->
				<exclusion>
					<groupId>com.caixabank.absis.arch.cbk</groupId>
					<artifactId>tauxconnector-lib</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<dependency>
			<groupId>com.caixabank.absis.apps.dataservice.cbk.insuranceproducts.contract</groupId>
			<artifactId>GTSBCK-micro</artifactId>
			<version>${gtsbck-micro-contract.version}</version>
		</dependency>

		<!-- END LIBRERIAS CAIXA -->

		<!-- BEGIN OTRAS LIBRERIAS -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>javax.xml.bind</groupId>
			<artifactId>jaxb-api</artifactId>
		</dependency>
		<!-- END OTRAS LIBRERIAS -->

	</dependencies>

</project>
