datasource:
  enable: true
  connections:
    oracledb:
      password: ${vcap.services.cbk-apps-infraestructuras-arqdop-oracledb-database.credentials.password}
      driver-class-name: oracle.jdbc.OracleDriver
      tenant: ALL
      url: ${vcap.services.cbk-apps-infraestructuras-arqdop-oracledb-database.credentials.uri}
    IMSEPPC5:
      password: ${vcap.services.cbk-apps-channelspecific-lifein-imseppc5-database.credentials.password:secret}
      dialect: null
      jdbcDriver: com.ibm.db2.jcc.DB2Driver
      tenant: IMSEPPC5
      url: ${vcap.services.cbk-apps-channelspecific-lifein-imseppc5-database.credentials.uri:uri}
      username: ${vcap.services.cbk-apps-channelspecific-lifein-imseppc5-database.credentials.username:user}
    default:
      password: ${vcap.services.cbk-apps-channelspecific-lifein-default-database.credentials.password:secret}
      dialect: null
      jdbcDriver: com.ibm.db2.jcc.DB2Driver
      tenant: '01'
      url: ${vcap.services.cbk-apps-channelspecific-lifein-default-database.credentials.uri:uri}
      username: ${vcap.services.cbk-apps-channelspecific-lifein-default-database.credentials.username:user}
readonly-datasource:
  enable: true
  connections:
    rooracledb:
      password: ${vcap.services.cbk-apps-infraestructuras-arqdop-oracledb-database.credentials.password}
      jdbcDriver: oracle.jdbc.OracleDriver
      tenant: ALL
      url: ${vcap.services.cbk-apps-infraestructuras-arqdop-oracledb-database.credentials.uri}
    IMSEPPB:
      password: ${vcap.services.cbk-apps-db2-database.credentials.password}
      jdbcDriver: com.ibm.db2.jcc.DB2Driver
      tenant: IMSEPPB
      url: jdbc:db2://sysj.lacaixa.es:5106/DBPB
      username: ${vcap.services.cbk-apps-db2-database.credentials.username}
    rodefault:
      password: ${vcap.services.cbk-apps-channelspecific-lifein-default-database.credentials.password:secret}
      dialect: null
      jdbcDriver: com.ibm.db2.jcc.DB2Driver
      tenant: '01'
      url: ${vcap.services.cbk-apps-channelspecific-lifein-default-database.credentials.uri:uri}
      username: ${vcap.services.cbk-apps-channelspecific-lifein-default-database.credentials.username:user}
